# Ask the Oracle

A macOS desktop app that broadcasts a prompt to multiple AI chatbots simultaneously. Type your question once and send it to ChatGPT, Claude, and Gemini in parallel — each opening in its own browser window with the prompt pre-filled.

Built with Python and Tkinter, packaged as a native macOS app.
